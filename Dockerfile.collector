# OC-Monitor v3.0 Collector Dockerfile
FROM python:3.12-slim

# 设置工作目录
WORKDIR /app

# 复制依赖文件
COPY pyproject.toml .

# 安装 Python 依赖
RUN pip install --no-cache-dir -e .

# 复制应用代码
COPY agent/ ./agent/

# 设置环境变量
ENV PYTHONUNBUFFERED=1

# 启动命令
CMD ["python", "-m", "agent.collector_optimized"]
